<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>WePost - Social Media App</display-name>
    <description>Mạng xã hội WePost xây dựng bằng Java Servlet và WebSockets</description>

    <!-- 1. Cấu hình trang mặc định khi truy cập (Root URL /) -->
    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>

    <!-- Filters -->
    <filter>
        <filter-name>AdminFilter</filter-name>
        <filter-class>com.wepost.filter.AdminFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AdminFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>

    <!-- Listeners -->
    <listener>
        <listener-class>com.wepost.listener.VisitCounterListener</listener-class>
    </listener>

    <!-- 2. Cấu hình bảo mật lỗi: Tùy chỉnh Error Pages (Hạn chế lộ lỗi Tomcat) -->
    <error-page>
        <error-code>404</error-code>
        <location>/login.jsp</location> <!-- Lỗi 404 đá về login hoặc trang báo lỗi tùy chọn -->
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/login.jsp</location> <!-- Lỗi code server -->
    </error-page>

    <!-- 3. Thiết lập Thời gian duy trì Session Cache (Phút) -->
    <session-config>
        <session-timeout>30</session-timeout> <!-- Người dùng không hoạt động 30p sẽ bị văng ra -->
    </session-config>

</web-app>
